#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

case "${1:-}" in
  add)
    shift
    node "$DIR/../scripts/reading-add.mjs" "$@"
    ;;
  *)
    echo "Usage: reading add <url> [summary...]" >&2
    exit 1
    ;;
esac
